# Used by `npm run build` (including Capacitor Android). Do not use localhost here —
# on a phone, localhost is the device itself, not your dev machine.
REACT_APP_API_URL=https://backendrender-3-ehrl.onrender.com/api
